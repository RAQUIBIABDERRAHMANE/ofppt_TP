# First run this to add the edited file or new files :
```bash
git add .
```

# then this to make comments for what you did :
```bash
git commit -m  " make your comment here "
```

# then push the changes :
```bash
git push -u origin main
```